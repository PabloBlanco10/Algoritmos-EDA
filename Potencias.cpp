//
//  Potencias.cpp
//  
//
//  Created by Pablo Blanco Peris on 16/12/16.
//
//

#include <stdio.h>
#include <iostream>
using namespace std;


int mod(int a, int b){
    int r;
    if(((a > 31542) && (b != 0)) || ((a > 1) && (b > 15)))
        r = 0;
    else{
        int k
    }
        
    
    
}

bool resuelve(){
    long double a;
    long double b;
    int n;
    
    while((a != 0) && (b != 0)){
        cout << mod(a, b);
        cin >> a >> b;
    }
    return false;
}

int main(){
    while(resuelve());
    return 0;
}